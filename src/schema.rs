table! {
    bj_products (product_id) {
        product_id -> Varchar,
        name -> Varchar,
        subhead -> Varchar,
        description -> Varchar,
        rating -> Double,
        rating_count -> Integer,
        ingredients -> Varchar,
    }
}
